.row
  .span4.search
    = render "search", search: @search

  .span8.content
    h4 Schemes

  - if @schemes.any?
    ul.schemes
      - @schemes.each do |scheme|
        = render "scheme", scheme: scheme
  - else
    p Please choose search criteria
